---
"@ceebee/ui": minor
---

Schedule draws dual bars: a planned track against the actuals (`ScheduleItem.actual`), an overrun/late danger tone, an optional `%` reading (`percentLabels`, on by default), and localised actual/not-started/overrun/late strings. Planned-only rows render exactly as before.
