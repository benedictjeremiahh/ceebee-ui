---
"@ceebee/ui": patch
---

Light links step down to the darker brand ramp step. Body-text links also sit on the subtle surface cards paint, and there the old step read 4.48–4.49:1 — under WCAG AA, found by a consumer measuring a board card the seed surfaces all passed. The subtle surface joins the link-contrast spec's surface list (emitted per skin by the token generator, so it cannot drift), and `tone-brand` is untouched: fills keep their step, only link text moves.
