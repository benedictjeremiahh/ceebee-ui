import { StepsShowcase } from '../../../components/navigation-showcases';
import { ApiReference } from '../../../components/api-reference';

# Steps

Displays progress through a sequence of tasks.

## When to use

Use Steps when a process has multiple ordered stages and the user needs progress, status, or navigation feedback.

## Examples

<StepsShowcase />

## API

`Steps` and `StepProps` are exported from `@ceebee/ui/client`.

<ApiReference component="Steps" />
