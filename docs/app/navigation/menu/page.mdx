import { MenuShowcase } from '../../../components/navigation-showcases';
import { ApiReference } from '../../../components/api-reference';

# Menu

A navigation menu for pages and operations.

## When to use

Use Menu for persistent top-level, inline, collapsed, vertical, or themed navigation structures.

## Examples

<MenuShowcase />

## API

`Menu`, `Menu.Item`, `Menu.SubMenu`, `Menu.ItemGroup`, and their props are exported from `@ceebee/ui/client`.

<ApiReference component="Menu" />
