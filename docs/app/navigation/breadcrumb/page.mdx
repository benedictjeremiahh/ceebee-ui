import { BreadcrumbShowcase } from '../../../components/navigation-showcases';
import { ApiReference } from '../../../components/api-reference';

# Breadcrumb

Displays the current location within a hierarchy.

## When to use

Use Breadcrumb when the product has more than two hierarchy levels and users may need to move upward.

## Examples

<BreadcrumbShowcase />

## API

`Breadcrumb`, `Breadcrumb.Item`, `Breadcrumb.Separator`, and their props are exported from `@ceebee/ui/client`.

<ApiReference component="Breadcrumb" />
