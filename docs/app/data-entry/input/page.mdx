import { InputShowcase } from '../../../components/data-entry/input/showcase';
import { ApiReference } from '../../../components/api-reference';

# Input

Through mouse or keyboard input content, it is the most basic form field wrapper.

## When to use

- A user input in a form field is needed.
- A search input is required.

## Examples

<InputShowcase />

## API

`Input`, `Input.TextArea`, `Input.Search`, `Input.Password`, and `Input.OTP` are exported from `@ceebee/ui/client`.

<ApiReference component="Input" />
