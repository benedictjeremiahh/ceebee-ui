import { FormShowcase } from '../../../components/data-entry/form/showcase';
import { ApiReference } from '../../../components/api-reference';

# Form

High-performance form component with data domain management. Includes data entry, validation, and corresponding styles.

## When to use

- When you need to create an instance or collect information.
- When you need to validate fields in certain rules.

## Examples

<FormShowcase />

## API

`Form`, `Form.Item`, `Form.List`, and `Form.Provider` are exported from `@ceebee/ui/client`.

<ApiReference component="Form" />
