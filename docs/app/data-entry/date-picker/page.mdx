import { DatePickerShowcase } from '../../../components/data-entry/date-picker/showcase';
import { ApiReference } from '../../../components/api-reference';

# DatePicker

To select or input a date.

## When to use

By clicking the input box, you can select a date from a popup calendar.

## Examples

<DatePickerShowcase />

## API

`DatePicker`, `DatePicker.RangePicker`, and the picker panels are exported from `@ceebee/ui/client`.

<ApiReference component="DatePicker" />
