import { TimePickerShowcase } from '../../../components/data-entry/time-picker/showcase';
import { ApiReference } from '../../../components/api-reference';

# TimePicker

To select/input a time.

## When to use

By clicking the input box, you can select a time from a popup panel.

## Examples

<TimePickerShowcase />

## API

`TimePicker` and `TimePicker.RangePicker` are exported from `@ceebee/ui/client`.

<ApiReference component="TimePicker" />
