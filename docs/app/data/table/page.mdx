import { TableDemo, PaginationDemo } from '../../../components/table-demos';
import { PropsTable, Guidance } from '../../../components/props-table';

# Table · Pagination <span className="docs__label">Composition</span>

A table, not a data grid: it renders rows and sorts by a column. No virtualisation, no column
resizing, no inline editing — those are product features, and a design system that grows them
becomes a framework nobody can leave.

## Usage

<TableDemo />

## Sorting has three states

Clicking a new column sorts ascending, clicking again flips it, and clicking a third time clears
the sort. That third state matters: "put it back the way the server gave it to me" is something
people look for, and two-state sorting hides it.

The state lives on the header as `aria-sort`, not only in the icon — a screen reader announces the
sort without seeing the arrow.

<Guidance
  do="Keep sorting controlled: the table reports what was clicked and your data layer decides what that means."
  dont="Sort inside the table over a paginated set — you would only be sorting the page in view."
/>

## Pagination

<PaginationDemo />

The page list always keeps the first and last page reachable, and a single skipped page is spelled
out rather than hidden behind an ellipsis that costs the same width. An empty set reads `0–0 of 0`
rather than `1–0`.

## Props

<PropsTable rows={[
  { name: 'columns', type: 'Array<Column<Row>>', description: 'key, header, cell(row), align, width, sortable, secondary.' },
  { name: 'rows / rowKey', type: 'Row[] / (row) => string', description: 'A stable key per row; index keys break every time the sort changes.' },
  { name: 'label', type: 'string', description: 'Required accessible name for the table.' },
  { name: 'sort / onSortChange', type: 'SortState | null', description: 'Controlled. nextSort() is exported if you drive it yourself.' },
  { name: 'onRowClick', type: '(row) => void', description: 'Makes rows clickable; keep a real control in the row too, for keyboards.' },
  { name: 'empty', type: 'ReactNode', description: 'Replaces the table entirely when there are no rows — an Empty fits here.' },
  { name: 'Column.secondary', type: 'boolean', description: 'Hides the column below 720px, for what a phone can live without.' },
  { name: 'Column.truncate', type: 'boolean', description: 'Clips overflowing text with an ellipsis instead of letting one long value widen the column.' },
]} />
