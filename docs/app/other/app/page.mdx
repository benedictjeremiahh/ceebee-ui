import { AppShowcase } from '../../../components/other/app/showcase';
import { ApiReference } from '../../../components/api-reference';

# App

Application wrapper for some global usages.

## When to use

- Provide reset styles based on `.ant-app` element.
- You could use static methods of `message/notification/Modal` from `useApp` without writing `contextHolder` manually.

## Examples

<AppShowcase />

## API

`App` and `App.useApp` are exported from `@ceebee/ui/client`.

<ApiReference component="App" />
