import { ConfigProviderShowcase } from '../../../components/other/config-provider/showcase';
import { ApiReference } from '../../../components/api-reference';

# ConfigProvider

Provide a uniform configuration support for components.

## When to use

Provide a uniform configuration support for components.

## Examples

<ConfigProviderShowcase />

## API

`ConfigProvider`, `ConfigProvider.config`, and `ConfigProvider.useConfig` are exported from `@ceebee/ui/client`.

<ApiReference component="ConfigProvider" />
