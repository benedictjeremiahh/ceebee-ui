import { FloatButtonShowcase } from '../../../components/general-showcases';
import { ApiReference } from '../../../components/api-reference';
import { Guidance } from '../../../components/props-table';

# FloatButton

A button that floats at the top of the page.

## When to use

- For global functionality on the site.
- For actions that must remain available wherever the user browses.

## Examples

<FloatButtonShowcase />

## API

`FloatButton`, `FloatButton.Group`, `FloatButton.BackTop`, and their props are exported from `@ceebee/ui/client`.

<ApiReference component="FloatButton" />
