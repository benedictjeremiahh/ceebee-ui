import { TimeDemo } from '../../../components/batch5-demos';
import { PropsTable, Guidance } from '../../../components/props-table';

# TimePicker <span className="docs__label">Composition</span>

The DatePicker's sibling. Typing accepts what people actually type; the list is a convenience on
top of that, never the only way to answer.

## Usage

<TimeDemo />

## What the parsing accepts

`9`, `9:30`, `09.30`, `0930`, `9pm`, `21:05`. A 12-hour suffix shifts the hour, `12am` becomes
`00:00`, and anything that would not be a real time — `25:00`, `9:75` — returns null rather than
being rounded into something plausible.

<Guidance
  do="Set step to the granularity your product actually books in; typing is never restricted to it."
  dont="Use it for a duration — '90 minutes' is not a time of day, and the picker will read as one."
/>

## Props

<PropsTable rows={[
  { name: 'value / defaultValue', type: 'TimeValue | null', description: '{ hours, minutes } in 24-hour form. null is an empty field.' },
  { name: 'onValueChange', type: '(value: TimeValue | null) => void', description: 'Fires on blur and on picking from the list.' },
  { name: 'min / max', type: 'TimeValue', description: 'Inclusive bounds. They also bound the offered list.' },
  { name: 'step', type: 'number', default: '30', description: 'Minutes between offered times.' },
  { name: 'size / disabled / invalid', type: '—', description: 'As every other control.' },
]} />

## Opening

Clicking the field opens the list, and so does the clock button — the button is a second way in,
not the only one. Typing is unaffected either way.
