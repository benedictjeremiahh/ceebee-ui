import { Demo } from '../../../components/demo';
import { PropsTable, Guidance } from '../../../components/props-table';
import { Skeleton, Statistic, Flex, Surface, Text } from '@ceebee/ui';

# Skeleton <span className="docs__label">Atom</span>

Shapes for the free-form case, and a matching `.Skeleton` on every Composition. The second half is
the point: a placeholder built from the same tokens as the real thing cannot drift out of sync with
it, so the page does not jump when data lands.

## Shapes

<Demo layout="block" code={`<Skeleton width="12rem" height="1rem" />
<Skeleton.Text lines={3} />
<Skeleton.Circle size="3rem" />`}>
  <Flex gap={4}>
    <Skeleton width="12rem" height="1rem" />
    <Skeleton.Text lines={3} />
    <Skeleton.Circle size="3rem" />
  </Flex>
</Demo>

## Composition skeletons

<Demo code={`{loading ? <Statistic.Skeleton withVisual /> : <Statistic {...stat} />}`}>
  <Statistic.Skeleton />
  <Statistic.Skeleton withVisual />
</Demo>

<Guidance
  do="Reach for the Composition's own .Skeleton, so the placeholder keeps the real layout's size."
  dont="Hand-assemble grey boxes that approximate the card — they will be wrong the first time the card changes."
/>

## Props

<PropsTable rows={[
  { name: 'width / height', type: 'string', default: "'100%' / '1rem'", description: 'Any CSS length. Prefer rem so it scales with type.' },
  { name: 'radius', type: "'sm' | 'md' | 'lg' | 'full'", default: "'md'", description: 'Corner token.' },
  { name: 'Skeleton.Text lines', type: 'number', default: '3', description: 'Line count; the last line is short, as real text is.' },
  { name: 'Skeleton.Text lastLineWidth', type: 'string', default: "'62%'", description: 'Width of that last line.' },
  { name: 'Skeleton.Circle size', type: 'string', default: "'2.5rem'", description: 'Diameter — for avatars and rings.' },
]} />

## Accessibility and motion

Every shape is `aria-hidden`, so a screen reader hears the loading state from your live region
rather than from a wall of empty boxes. The shimmer is CSS and stops under
`prefers-reduced-motion` — the shapes stay, which is what communicates "loading".
