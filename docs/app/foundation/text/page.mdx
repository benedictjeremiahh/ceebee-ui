import { Demo } from '../../../components/demo';
import { PropsTable, Guidance } from '../../../components/props-table';
import { Text, Heading, Stack } from '@ceebee/ui';

# Text · Heading <span className="docs__label">Atom</span>

## Heading

`level` is document structure and `size` is appearance, and they are separate props on purpose: a
page often needs an `h2` that looks small, and demoting it to `h3` to get that would damage the
outline a screen reader reads.

<Demo layout="block" code={`<Heading level={2} size="lg">Section that looks small</Heading>`}>
  <Stack gap={2}>
    <Heading level={1}>Heading level 1</Heading>
    <Heading level={2}>Heading level 2</Heading>
    <Heading level={2} size="lg">Level 2, size lg</Heading>
  </Stack>
</Demo>

## Text

<Demo layout="block" code={`<Text size="sm" tone="muted">Supporting copy</Text>
<Text numeric>120,456.50</Text>`}>
  <Stack gap={2}>
    <Text>Default body copy</Text>
    <Text tone="muted" size="sm">Muted, small</Text>
    <Text tone="subtle" size="xs">Subtle, extra small</Text>
    <Text tone="danger" size="sm">Danger</Text>
    <Text numeric weight="semibold">120,456.50 — tabular numerals</Text>
  </Stack>
</Demo>

<PropsTable rows={[
  { name: 'size', type: "'xs' | 'sm' | 'md' | 'lg'", default: "'md'", description: 'Type scale step.' },
  { name: 'tone', type: "Tone | 'muted' | 'subtle'", default: "'neutral'", description: 'Semantic colour, or the two reading-level greys.' },
  { name: 'weight', type: "'regular' | 'medium' | 'semibold'", default: "'regular'", description: 'Weight token.' },
  { name: 'numeric', type: 'boolean', default: 'false', description: 'Tabular numerals — use it for anything in a column that updates.' },
  { name: 'as', type: "'p' | 'span' | 'div'", default: "'p'", description: 'Rendered element.' },
]} />

<Guidance
  do="Turn on numeric for money, counts, and anything that changes in place."
  dont="Leave proportional numerals in a table column — the digits shift sideways on every update."
/>
