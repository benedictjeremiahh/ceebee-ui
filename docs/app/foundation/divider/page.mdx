import { Demo } from '../../../components/demo';
import { PropsTable, Guidance } from '../../../components/props-table';
import { Divider, Flex, Text } from '@ceebee/ui';

# Divider <span className="docs__label">Atom</span>

A rule between things.

## Usage

<Demo layout="block" code={`<Divider />`}>
  <Flex gap={0}>
    <Text tone="muted">Everything about the account.</Text>
    <Divider />
    <Text tone="muted">Everything about billing.</Text>
  </Flex>
</Demo>

## With a label

An `<hr>` when it separates content, because that is what the element means and a screen reader
announces it. With a label it becomes a `<div>` carrying the rule in its borders instead: an `<hr>`
may not contain anything, and a separator that says which section is starting is worth more than
the element. Either way it announces itself as a separator.

<Demo layout="block" code={`<Divider>Or</Divider>`}>
  <Flex gap={0}>
    <Text tone="muted">Continue with your email address.</Text>
    <Divider>Or</Divider>
    <Text tone="muted">Continue with a single-sign-on provider.</Text>
  </Flex>
</Demo>

## Orientation

Vertical stretches to its flex row and takes side margins instead of block ones, so it separates
controls sitting beside one another.

<Demo code={`<Flex direction="row" align="center">
  <Text>Draft</Text>
  <Divider orientation="vertical" />
  <Text>Edited 4m ago</Text>
  <Divider orientation="vertical" />
  <Text>2 reviewers</Text>
</Flex>`}>
  <Flex direction="row" align="center" gap={0}>
    <Text tone="muted">Draft</Text>
    <Divider orientation="vertical" />
    <Text tone="muted">Edited 4m ago</Text>
    <Divider orientation="vertical" />
    <Text tone="muted">2 reviewers</Text>
  </Flex>
</Demo>

## Props

<PropsTable rows={[
  { name: 'children', type: 'ReactNode', description: 'A word or two sitting in the rule.' },
  { name: 'orientation', type: "'horizontal' | 'vertical'", default: "'horizontal'", description: 'Vertical stretches to its flex row and takes side margins instead of block ones.' },
]} />

<Guidance
  do="Use it between sections of one thing."
  dont="Use it to separate cards that already have their own edges — two boundaries where there is one break."
/>
