import { MasonryShowcase } from '../../../components/layout-showcases';
import { ApiReference } from '../../../components/api-reference';

# Masonry

A responsive masonry layout for items with varied heights.

## When to use

Use Masonry for independent cards, images, or content blocks that intentionally form ragged columns.

## Examples

<MasonryShowcase />

## API

`Masonry` and `MasonryProps` are exported from `@ceebee/ui/client`.

<ApiReference component="Masonry" />
