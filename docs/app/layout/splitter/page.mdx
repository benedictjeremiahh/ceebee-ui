import { SplitterShowcase } from '../../../components/layout-showcases';
import { ApiReference } from '../../../components/api-reference';

# Splitter

Divide a region into resizable panels.

## When to use

Use Splitter when adjacent persistent regions need user-controlled relative space.

## Examples

<SplitterShowcase />

## API

`Splitter`, `Splitter.Panel`, and their props are exported from `@ceebee/ui/client`.

<ApiReference component="Splitter" />
