import { SpaceShowcase } from '../../../components/layout-showcases';
import { ApiReference } from '../../../components/api-reference';

# Space

Set consistent spacing between components.

## When to use

Use Space for equal horizontal or vertical gaps, wrapping, separators, and compact control groups.

## Examples

<SpaceShowcase />

## API

`Space`, `Space.Compact`, and their props are exported from `@ceebee/ui/client`.

<ApiReference component="Space" />
