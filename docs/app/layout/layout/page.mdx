import { LayoutShowcase } from '../../../components/layout-showcases';
import { ApiReference } from '../../../components/api-reference';

# Layout

The page frame composed from Layout, Header, Sider, Content, and Footer.

## Component overview

Layout provides the wrapper; Header, Sider, Content, Footer, and nested Layout instances compose the application frame.

## Examples

<LayoutShowcase />

## API

`Layout` and its subcomponents are exported from `@ceebee/ui/client`.

<ApiReference component="Layout" />
