import { DividerShowcase } from '../../../components/layout-showcases';
import { ApiReference } from '../../../components/api-reference';
import { Guidance } from '../../../components/props-table';

# Divider

A divider line separates different content.

## When to use

- Divide sections of an article or page.
- Divide inline text and links with the vertical treatment.

## Examples

<DividerShowcase />

## API

`Divider` and `DividerProps` are exported from `@ceebee/ui/client`.

<ApiReference component="Divider" />

## Also available

The server-safe Ceebee `Divider` remains at `@ceebee/ui`; use `@ceebee/ui/client` for the complete contract.
