import { AvatarShowcase } from '../../../components/data-display/avatar/showcase';
import { ApiReference } from '../../../components/api-reference';

# Avatar

Used to represent users or things, supporting the display of images, icons, or characters.

## When to use

Used to represent users or things, supporting the display of images, icons, or characters.

## Examples

<AvatarShowcase />

## API

`Avatar` and `Avatar.Group` are exported from `@ceebee/ui/client`.

<ApiReference component="Avatar" />
