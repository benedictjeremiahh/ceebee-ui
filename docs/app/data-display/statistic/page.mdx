import { StatisticShowcase } from '../../../components/data-display/statistic/showcase';
import { ApiReference } from '../../../components/api-reference';

# Statistic

Display statistic number.

## When to use

- When want to highlight some data.
- When want to display statistic data with description.

## Examples

<StatisticShowcase />

## API

`Statistic`, `Statistic.Timer`, and `Statistic.Countdown` are exported from `@ceebee/ui/client`.

<ApiReference component="Statistic" />
